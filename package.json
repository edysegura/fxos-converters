{
	"devDependencies": {
		"karma": "0.10.2"
	},

	"repository": {
		"type": "git",
		"url": "https://github.com/danielpsf/fxos-converters.git"
	},

	"scripts": {
		"test": "./node_modules/.bin/karma start --browser Firefox --single-run"
	}
}
